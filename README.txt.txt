由于本程序设计客户端与服务器的通信，故使用前先打开客户端源代码，将utils文件下的init_socket函数中的ip设成您打算将服务器部署的设备所在局域网ip。
同时，用VS打开服务器源码，将项目的C++附加库包含路径中sqlite的路径改成在您电脑上的新路径。

客户端、服务器的源码分别在client_test和server中；客户端的打包程序在client_test/客户端/Debug下，需将程序安装到本地指定路径。